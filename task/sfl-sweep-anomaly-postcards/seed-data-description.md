The task starts with a fictional MLOps estate for Saffron Ledger AI.

- W&B contains ten product projects and 446 runs whose state is `killed`.
- Every run has a visible internal run name, immutable run ID, `estimated_cost_usd` summary metric, and `gpu_type` configuration value.
- The twelve company-wide highest-cost killed runs are distributed across several product projects; there is no tie at the selection boundary.
- Hugging Face contains one model repository per product. Existing repository discussions are preserved and are not part of the requested output.
- The expected end state adds exactly twelve new discussions: one for each qualifying run and no others. W&B is read-only.

All company names, accounts, URLs, and credentials are fictional simulation data.
